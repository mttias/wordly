import React, { useEffect, useState } from "react";

const ExperiencePoints = (prop) => {
  return (
    <div>
      <div>{prop}</div>
    </div>
  );
};

// updateExperiencePoints(experiencePoints + (20 + Math.floor(getRandomInt(40))));

export default ExperiencePoints;
